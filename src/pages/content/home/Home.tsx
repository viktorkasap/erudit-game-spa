import { Button } from '@mantine/core';

import { Board } from 'widgets/board';

const Home = () => {
  return (
    <>
      <Button>Button</Button>
      <Board />
    </>
  );
};

export default Home;
